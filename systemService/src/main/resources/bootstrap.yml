spring:
  application:
    name: system
  cloud:
    config:
      uri: http://localhost:8769
      enabled: true
      profile: dev
      fail-fast: true
logging:
  level:
    com.example.demo.dao: debug
  file:
    max-size: 100MB
    max-history: 60
  path: /home/leve/logs/${spring.application.name}

